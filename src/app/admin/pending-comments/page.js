import getComments from "@/utils/getComments.mjs";

const page = async () => {
    // const comments = await getComments(1,100000,'newest','','',true)
    // getComments = async (page, limit, sort, blog_id, approvedOnly, pendingOnly, all, keyword="")
    return (
        <div>
            
        </div>
    );
};

export default page;